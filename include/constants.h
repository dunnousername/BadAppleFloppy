#ifndef CONSTANTS_H
#define CONSTANTS_H

/* This is where you define constants that configure the animation */

/* The total frame count */
#define MAX_FRAME 7779

/* size of each frame 
 * (probably can't change these and expect things to work)
 */
#define MAX_X (320)
#define MAX_Y (240)
#define FRAME_SIZE (MAX_X*MAX_Y)

#endif